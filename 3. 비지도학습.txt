감마값이 증가하면 개별 포인트에 집중

c가 커지면 제약이 작아짐


스케일 편차가 큼=학습효과가 떨어짐?

스케일링 X(i)-X(min)/X(max)-X(min)
	i가 X(min)이면 값은 0
	i가 X(max)이면 값은 1
	
X_test_scaled의 분모가 range_on_training인 이유 : 학습데이터와 테스트데이터의 값을 동일하게 셋팅하기 위해서


비지도학습
	정답없이 하는 모든 데이터

전처리 :  사전작업, 이 경우 데이터를 분석하기 쉽게 만드는 작업을 의미함

    변환
        차원축소: 특성(f )의 수를 줄여서 필요한 특성만 남김 => 2차원 데이터화


군집

    k-평균 군집
	1. 임의의 포인트 k개 설정
	2.영역설정 및 영역의 중심 찾기
	3. 중심으로 이동 및 기점으로 영역 재설정
	4.중심의 변화가 없을 때 까지 반복

    병합 군집
	모든 포인트를 클러스터로 지정, 비슷한 클러스터끼리 병함
	종료조건 만족전까지 반복

    DBSCAN(밀도 기반 클러스터링)
	eps: 확인 거리
	min_samples: 필요 포인트 수
	
	eps안의 min_samples 갯수를 확인하여 묶음

핵심 경계 잡음
